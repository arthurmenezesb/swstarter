import React from "react";
import { Typography } from "antd";

const { Title } = Typography;

const Home: React.FC = () => {
  return (
    <div>
      <Title>SWStarter</Title>
    </div>
  );
};

export default Home;
